---
import styles from "./Chip.module.css";

export interface Props {
  type?: string;
}

const { type } = Astro.props;
---

<div data-object-type={type ?? "default"} class={styles.container}>
  <slot />
</div>
